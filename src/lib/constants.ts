// Fixed UUID for the IARIS internal Kanban board.
// Stored as a portfolio_startups record with is_system = true so it
// never appears in portfolio listings but can hold kanban_tasks normally.
export const IARIS_STARTUP_ID = '00000000-0000-0000-0000-000000000001'

// Sentinel quarter value for IARIS-internal tasks (no quarter concept).
export const IARIS_QUARTER = 'iaris'
